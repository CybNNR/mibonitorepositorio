|*Estructuras de datos y algoritmos*|

Big O notation: Tiempo de ejecucion de un algoritmo.
Algoritmos: Recetas o pasos a seguir para lograr crear algo o llegar a un fin
Estructuras de Datos: Contenedores de informacion la cual sera usada en un algoritmo

Tiempo comun de ejecucuciones:
O(N²): Insertionsort, Selectionsort, Bubblesort
O(N Log(N)): Quicksort, Mergesort, Heapsort
O(Log(N)): Bucketsort, Countingsort


Memory Management in C++

Memory Leaks
Se crean elementos en el heap con los atributos "new", el no eliminar estos atributos hace que se creen memory leaks, para resolver esta problematica, se debe de hacer uso del destructor "delete"

Forma correcta de crear y eliminar variables o en este caso apuntadores a arreglos
int* arr = new int[];
delete [] Pointervariable;

Segmentation Faults:
Se crean en los siguientes casos:
    Intento de desreferenciar apuntadores nulos
    Intento de acceder a memoria restringida o elementos que fueron eliminados o que no existen
    Modificacion de memoria de solo escritura, comunmente cuando se inrenta modificar un string literal
    Creacion de arreglos demasiado grandes (Stack overflow) -> Lleva a la corrupcion de memoria

|Linkedlists| ejercicio en clase (Vease practica en C++)
|LinkedlistSingle| Ejercicio del examen, encontrar el valor minimo(Vease practica de C++)
|Reversed_Single_Linkedlist| Ejercicio del overview (Vease practica de C++ mas imagen de ArielOrtiz.info)

|°Stacks y queues°|

Stack: Pila, basado en el principio last in, first out o LIFO, esta es un contenedor estandar de la biblioteca de c++, aunque el siguiente ejercicio sera exclusivamente de aquellos vistos en clase, mas adelante se vera los de los contenedores de la biblioteca standard de c++

Cuenta con las siguientes operaciones: 
    Capacity
    Size
    Isempty
    push
    pop
    clear
    peak

|Stacks| Ejercicio en clase (Vease practica de c++)

Queue: Cola basada en el principio de First in First out, es un contenedor que aparece en la biblioteca standard de c++, anque el siguiente ejercicio vera aquel visto en clase, mas adelante se vera los contenedores de la biblioteca standard de c++

Cuentan con las siguientes operaciones:
    capacity
    clear
    dequeue
    enqueue
    isempty
    peek
    size

|Queues| Ejercicio en clase (Vease practica de c++)

|Standard containers in C++|

std::deque
        -------------------------
back--->|   |   |   |   |   |   | <---front
        -------------------------
    Operations:
        Element access: 
            at
            front 
            back
        Capacity:
            empty
            size
            max_size
        Modifiers:
            insert
            push_back
            pop_back
            push_front
            pop_front
            swap

std::list

std::fowardlist

std::vector

std::array

std::queue

|-°Sorting Algorithms°-|

O(N²): Insertionsort, Selectionsort, Bubblesort
O(N Log(N)): Quicksort, Mergesort, Heapsort
O(Log(N)): Bucketsort, Countingsort

Insertion sort: Funciona creando un miniarreglo donde los elementos que se encuentran a la izquierda se va comparando para acomodar cada elemento entrante a la derecha, comparando elemento a elemento con en el mini arreglo de la derecha  de ahi O(N²), ya que este tiene que recorrer todos los elementos cada que pasa con un elemento. (insercion en miniarreglo)

|Insertion Sort|: Vease en practica de C++

Selectionsort: Se basa en el escaneo repetido de una porcion no ordenada de arreglo para encontrar el elemento mas pequeño y luego intecambendolo a la primera posicion de la seccion no ordenada, compara los elementos de los demas ,encuentra el mas pequeño y lo manda al inicio, de ahi con los demas va encontrando el mas pequeño y lo manda al inicio con el segundo mas pequeño

|Selection sort|: vease en la practica de c++

Bubble sort: 

Mergesort:

Quicksort:

Heapsort: 

Countingsort: 

Bucketsort:

|*---Algoritmo de ordenamiento de la biblioteca estandard de c++: std::sort---*|

